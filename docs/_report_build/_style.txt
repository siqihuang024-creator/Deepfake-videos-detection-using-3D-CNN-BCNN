<style>
  :root {
    --paper: #F4F5F7; --surface: #FFFFFF; --ink: #14171B; --muted: #565E68;
    --faint: #878F99; --rule: #DBE0E6; --rule-soft: #EAEDF1;
    --s1: #2a78d6; --s2: #eb6834; --s3: #1baf7a; --accent: #1f5fae;
    --serif: "Source Serif 4", "Noto Serif SC", Georgia, "Songti SC", serif;
    --mono: "IBM Plex Mono", ui-monospace, SFMono-Regular, Menlo, monospace;
  }
  @media (prefers-color-scheme: dark) {
    :root:not([data-theme="light"]) {
      --paper: #101317; --surface: #171B20; --ink: #E8EBEE; --muted: #99A2AC;
      --faint: #6C757F; --rule: #2A3038; --rule-soft: #212730;
      --s1: #3987e5; --s2: #d95926; --s3: #199e70; --accent: #6ba8ee;
    }
  }
  :root[data-theme="dark"] {
    --paper: #101317; --surface: #171B20; --ink: #E8EBEE; --muted: #99A2AC;
    --faint: #6C757F; --rule: #2A3038; --rule-soft: #212730;
    --s1: #3987e5; --s2: #d95926; --s3: #199e70; --accent: #6ba8ee;
  }
  body {
    background: var(--paper); color: var(--ink);
    font-family: var(--serif); font-size: 16.5px; line-height: 1.78;
    -webkit-font-smoothing: antialiased;
  }
  .page { max-width: 860px; margin: 0 auto; padding: 60px 26px 110px; }
  header { text-align: center; border-bottom: 1px solid var(--rule); padding-bottom: 30px; margin-bottom: 34px; }
  h1 { font-size: 30px; font-weight: 600; line-height: 1.3; margin: 0 0 10px; text-wrap: balance; }
  .sub { color: var(--muted); font-size: 15px; margin: 0; }
  .meta { font-family: var(--mono); font-size: 11.5px; color: var(--faint); margin-top: 14px; letter-spacing: .04em; }
  h2 {
    font-size: 19px; font-weight: 600; margin: 46px 0 4px;
    display: flex; align-items: baseline; gap: 11px; text-wrap: balance;
  }
  h2 .n { font-family: var(--mono); font-size: 13px; color: var(--accent); flex: none; }
  h3 { font-size: 16.5px; font-weight: 600; margin: 30px 0 2px; }
  p { margin: 12px 0; }
  strong { font-weight: 600; }
  code, .m { font-family: var(--mono); font-size: .86em; }
  code { background: var(--rule-soft); padding: 1px 5px; border-radius: 3px; }
  .abstract { background: var(--surface); border: 1px solid var(--rule); border-radius: 4px; padding: 20px 24px; }
  .abstract p { margin: 0; font-size: 15.5px; }
  .abstract .lead {
    font-family: var(--mono); font-size: 11px; letter-spacing: .13em;
    text-transform: uppercase; color: var(--accent); margin-bottom: 9px;
  }
  figure { margin: 26px 0 6px; }
  .figbox {
    background: var(--surface); border: 1px solid var(--rule);
    border-radius: 4px; padding: 16px 12px 10px; overflow-x: auto; position: relative;
  }
  figure svg { display: block; width: 100%; height: auto; min-width: 560px; }
  figcaption { font-size: 13.5px; color: var(--muted); margin-top: 11px; line-height: 1.6; }
  figcaption b { color: var(--ink); font-weight: 600; }
  .tw { overflow-x: auto; margin: 20px 0; }
  table { border-collapse: collapse; width: 100%; font-size: 14.5px; min-width: 480px; }
  caption { caption-side: top; text-align: left; font-size: 13.5px; color: var(--muted); padding-bottom: 8px; }
  caption b { color: var(--ink); font-weight: 600; }
  th, td { padding: 8px 14px 8px 0; border-bottom: 1px solid var(--rule-soft); text-align: left; }
  thead th {
    font-family: var(--mono); font-size: 11px; letter-spacing: .08em; text-transform: uppercase;
    color: var(--faint); font-weight: 500; border-bottom: 1px solid var(--rule);
  }
  td.n, th.n { text-align: right; font-family: var(--mono); font-variant-numeric: tabular-nums; }
  tbody tr:last-child td { border-bottom: none; }
  tr.head td { font-weight: 600; }
  .key { color: var(--s2); font-weight: 600; }
  .dim { color: var(--faint); }
  .note { border-left: 2px solid var(--accent); padding: 4px 0 4px 18px; margin: 22px 0; color: var(--muted); font-size: 15px; }
  .note b { color: var(--ink); }
  .tip {
    position: absolute; pointer-events: none; opacity: 0;
    background: var(--ink); color: var(--surface);
    font-family: var(--mono); font-size: 11.5px; line-height: 1.5;
    padding: 5px 9px; border-radius: 3px; white-space: nowrap;
    transform: translate(-50%, -125%); transition: opacity .1s;
  }
  hr { border: none; border-top: 1px solid var(--rule); margin: 50px 0 0; }
  footer { color: var(--faint); font-size: 13px; margin-top: 18px; }
  a { color: var(--accent); }
</style>