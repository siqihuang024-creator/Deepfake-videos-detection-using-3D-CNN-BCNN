# -*- coding: utf-8 -*-
"""The primer's text. Kept apart from the layout so either can change alone."""


def blocks(P, BUL, CODE, ANCHOR, TBL, Spacer, PageBreak, WIDTH):
    out = []
    add = out.extend
    one = out.append

    # ------------------------------------------------------------------ cover
    one(Spacer(1, 34))
    one(P("深度学习与视频取证基础", "title"))
    one(P("从一个失败的检测器出发，把你缺的那部分知识补齐", "subtitle"))
    one(Spacer(1, 14))
    one(P("为 Deepfake Video 3D-CNN + BCNN 项目而写 · 2026 年 9 月", "meta"))
    one(Spacer(1, 22))
    add([P(
        "这份文档不是教科书的压缩版。它的每一节都锚在你这个项目里真实遇到那个概念的时刻——"
        "你问过的问题、卡住的地方、事后才发现填错的数字。先讲那件事，再展开背后的原理。"
        "这样读下来，概念是挂在你已经花过时间的具体经验上的，而不是悬空的定义。", "body")])
    add([P(
        "阅读顺序建议从第 0 章开始。它用两页把整个项目的流程捋一遍，"
        "后面每一章讲的都是这条流程上的某一个环节。如果某一章你已经懂了，跳过不影响后面。", "body")])
    one(Spacer(1, 16))

    add(TBL(["章", "讲什么", "对应你问过的问题"], [
        ["0", "项目全流程地图", "「这个实验在干嘛」"],
        ["1", "张量：数据的形状", "「时间维均值是对什么取平均」"],
        ["2", "卷积网络的四个部件", "「池化不是必须的吗」「就一个 batch？」"],
        ["3", "训练：模型怎么学", "「阶段 B 没有反向传播吧」「更新了 50 次吗」「KL 散度是什么」"],
        ["4", "评估：数字何时可信", "「留出测试是啥」「封存测试集的意义」"],
        ["5", "表征：特征好坏怎么测", "「特征表征测量是干什么的」"],
        ["6", "迁移学习与预训练", "「预训练怎么用」"],
        ["7", "数据、泄漏与捷径", "供体泄漏的整条线索"],
        ["8", "视频比图像多出来的", "3D 卷积、片段采样、视频级聚合"],
        ["9", "这个项目产出了什么", "—"],
    ], widths=[WIDTH * 0.07, WIDTH * 0.42, WIDTH * 0.51]))

    one(PageBreak())

    # -------------------------------------------------------------- chapter 0
    one(P("0　这个项目实际发生了什么", "h1"))
    add([P(
        "先把整条流程摊开。后面每一章都是在讲这条线上的某一段，你随时可以翻回这一页对位置。", "body")])

    one(P("0.1　任务", "h2"))
    add([P(
        "给一段视频，判断它是真实录像还是伪造的。伪造分三大类：<b>换脸</b>（把 A 的脸换到 B 的视频里）、"
        "<b>面部重演</b>（用 A 的表情驱动 B 的脸）、<b>语音驱动的说话人生成</b>（给一张脸和一段音频，"
        "生成对应口型的视频）。你用的两个数据集里，DFD 只有换脸，CelebDF++ 三类都有。", "body")])

    one(P("0.2　流程", "h2"))
    add([CODE([
        "原始视频 (mp4)",
        "   |  解码、抽帧、人脸检测与裁剪（或整帧隔点抽样）",
        "   v",
        "片段张量  3 x 8 x 256 x 256      <- 第 1 章",
        "   |  三段 3D 卷积 + 池化 + 归一化 + 激活",
        "   v",
        "特征向量  15488 维               <- 第 2 章",
        "   |",
        "   +-- 阶段 A：接一个确定性头，用真假标签监督训练    <- 第 3 章",
        "   |            训练完丢弃这个头，只留提取器权重",
        "   |",
        "   +-- 阶段 B：冻结提取器，只用真实视频训练贝叶斯头",
        "                输出异常分数",
        "   v",
        "视频级异常分数  ->  AUROC / EER / TPR@5%FPR   <- 第 4 章",
    ])])

    one(P("0.3　为什么要分两个阶段", "h2"))
    add([P(
        "最终目标是<b>单类异常检测</b>：只用真实视频训练，把偏离"
        "「真实视频长什么样」的输入判为伪造。这样做的好处是不依赖某一种具体的伪造方法，"
        "理论上对没见过的新伪造方法也能work。", "body")])
    add([P(
        "但直接这么训练有个现实问题：贝叶斯头的目标函数里除了分类项，还有一项 KL 散度，"
        "它的大小正比于参数数量，而这个头有 793 万个权重。早期五次运行里 KL 占了整个目标函数的 "
        "95–99.9%，训练卡在随机水平。所以拆成两步——先用真假标签把特征提取器训出来（阶段 A），"
        "再冻住它、只训贝叶斯头（阶段 B）。KL 到底是什么、以及「占 95%」是不是"
        "真的等于「分类信号被淹没」，见第 3.7 节——答案比这句话复杂。", "body")])

    one(P("0.4　结果", "h2"))
    add(TBL(["", "阶段 A 验证（最好/均值）", "阶段 B 验证（最好）", "留出测试"], [
        ["DFD 整帧", "0.6039 / 0.4477", "0.6627", "0.4877"],
        ["CelebDF++ 人脸裁剪", "0.6937 / 0.6572", "0.6569", "0.5284"],
    ]))
    add([P(
        "AUROC 的随机水平是 0.50。两个测试结果都贴着随机，也就是说这个检测器<b>没有展示出可靠的泛化能力</b>。"
        "第 3 到第 6 章会逐个讲清楚，为什么会这样，以及每一个环节本来应该怎么做。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 1
    one(P("1　张量：数据在网络里是什么形状", "h1"))
    add(ANCHOR(
        "你问过：「架构图里 temporal mean 这个框是在干什么？对什么取平均值？」"
        "这个问题的答案完全取决于你能不能在脑子里跟踪张量形状的变化。"
        "这一章讲的就是这件事——它是后面所有内容的地基。"))

    one(P("1.1　从视频到数字", "h2"))
    add([P(
        "一张彩色图像是三个二维数组：红、绿、蓝三个<b>通道</b>，每个通道是一个 高×宽 的矩阵，"
        "每个元素是 0 到 255 的整数（送进网络前一般归一化到 0–1 或标准化）。"
        "所以一张 256×256 的彩色图是 <b>3 × 256 × 256</b> 的数组，共 196,608 个数。", "body")])
    add([P(
        "视频多一个维度：时间。取连续 8 帧，就是 <b>3 × 8 × 256 × 256</b>。"
        "训练时一次送进去若干个这样的样本，最前面再加一个<b>批</b>维度，变成 "
        "<b>[N, C, T, H, W]</b>——这是 PyTorch 里 3D 卷积的标准约定："
        "批大小、通道、时间、高、宽。", "body")])
    add([P(
        "「张量」这个词没什么神秘的，它就是<b>多维数组</b>。一维叫向量，二维叫矩阵，"
        "三维以上统称张量。深度学习框架里所有数据都是张量。", "body")])

    one(P("1.2　卷积怎么改变形状", "h2"))
    add([P(
        "你的模型每一段卷积核是 <span>k_t × 5 × 5</span>（时间方向 3，空间方向 5×5）。"
        "空间上不做填充（padding=0），所以每做一次卷积，高和宽<b>各减少 4</b>（即 k−1）。"
        "时间方向填充了 1，所以时间长度保持不变。", "body")])
    add([P(
        "通道数由卷积核的数量决定，跟输入通道数无关。你的三段是 3→16→24→32，"
        "意思是第一段用 16 个卷积核，每个核都跨越输入的全部 3 个通道。", "body")])

    one(P("1.3　池化怎么改变形状", "h2"))
    add([P(
        "池化把一个局部窗口压成一个值。你的配置是窗口 4×4、步长 2，"
        "输出尺寸 = ⌊(输入 − 4) / 2⌋ + 1，大约是输入的一半。", "body")])

    one(P("1.4　把你的模型完整走一遍", "h2"))
    add([CODE([
        "输入          3 x 8 x 256 x 256",
        "  conv1  3->16, k=3x5x5      16 x 8 x 252 x 252    高宽各减 4",
        "  pool1  4x4 stride 2        16 x 8 x 125 x 125    高宽减半",
        "  conv2  16->24              24 x 8 x 121 x 121",
        "  pool2                      24 x 8 x  59 x  59",
        "  conv3  24->32              32 x 8 x  55 x  55",
        "  pool3                      32 x 8 x  26 x  26",
        "  时间维取均值                32 x  26 x  26        <- 8 消失了",
        "  自适应池化到 22x22          32 x  22 x  22",
        "  展平                        15488                 = 32 x 22 x 22",
    ])])
    add([P(
        "<b>这就是你那个问题的答案。</b>第三段卷积输出的是 32 个通道、<b>8 个时间位置</b>、"
        "每个位置一张 26×26 的特征图。「时间维取均值」是把这 8 张图逐像素平均成 1 张，"
        "于是时间维消失，一个 8 帧的片段最终只产生<b>一个</b>特征向量，而不是 8 个。", "body")])
    add([P(
        "为什么必须消掉时间维？因为后面接的是全连接层，它只接受固定长度的向量。"
        "而为什么选「取平均」这种消法、有没有别的选择，是第 8 章的内容。", "body")])

    one(P("1.5　自适应池化：为什么两种输入都得到 15488 维", "h2"))
    add([P(
        "整帧输入是 540×960，人脸裁剪是 256×256，尺寸差很多，但都得到 15488 维特征。"
        "秘密在 <span>AdaptiveAvgPool2d(22)</span>：普通池化你指定窗口大小，"
        "自适应池化你指定<b>输出</b>大小，它自己算窗口。所以 26×26 和 62×114 都会被压成 22×22。", "body")])
    add([P(
        "代价是整帧输入的 62×114 是个长方形，被强行压成正方形，长宽方向的压缩比例不同——"
        "这叫各向异性变形。项目里保留这个设计是为了让特征维度跟原论文一致，"
        "从而让贝叶斯头的参数量和 KL 项的量级不变。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 2
    one(P("2　卷积网络的四个基本部件", "h1"))
    add(ANCHOR(
        "你问过两个问题：「池化不是必须的吗？我总不能不池化吧」和「就一个 batch？合理吗？」。"
        "这两个问题分别落在这一章的第 2 节和第 3 节。它们都不是简单的是非题——"
        "必须做的事情和具体怎么做，是两个独立的决定。"))

    one(P("2.1　卷积：为什么不用全连接", "h2"))
    add([P(
        "假设直接把 3×256×256 的图展平成 196,608 维向量，接一个 1000 维的全连接层，"
        "参数量是 1.97 亿。而且这个做法有个根本缺陷：它把「左上角的像素」和「右下角的像素」"
        "当作完全无关的两个输入，图像的空间结构信息全部丢失。", "body")])
    add([P("卷积用两个假设解决这个问题：", "body")])
    add(BUL([
        "<b>局部性</b>：一个像素的意义主要由它周围的像素决定。所以卷积核只看 5×5 的窗口。",
        "<b>权值共享</b>：识别「边缘」这件事在图像的哪个位置都一样。所以同一个卷积核滑过整张图，"
        "参数只有 5×5×输入通道×输出通道 那么多。",
    ]))
    add([P(
        "你的三段卷积总共只有 <b>90,280</b> 个参数——对比上面那个 1.97 亿。"
        "这就是卷积的效率来源。", "body")])

    one(P("2.2　感受野：一个输出看到了多少输入", "h3"))
    add([P(
        "堆叠卷积层会扩大<b>感受野</b>——最终输出的一个数值受到多大范围输入的影响。"
        "第一层看 5×5，第二层的每个位置看第一层的 5×5，等效于看原图的 9×9，以此类推。", "body")])
    add([P(
        "时间方向同理。你的时间卷积核是 3，三层叠起来的<b>时间感受野是 7 帧</b>"
        "（1 + 3×(3−1) = 7）。也就是说，最终特征里的每个数值，最多能看到 7 帧的变化。"
        "这决定了模型能捕捉多长的时序模式。", "body")])

    one(P("2.3　池化：必须降采样，但不必须这样降", "h2"))
    add([P(
        "<b>降采样是必须的</b>，理由有两个：一是不降的话特征图太大，显存和计算量爆炸；"
        "二是降采样能扩大感受野，让深层看到更大范围。这一点你的直觉是对的。", "body")])
    add([P(
        "但「降采样」和「用 4×4 窗口做平均」是两个独立的决定。<b>平均池化在数学上等价于"
        "用一个均匀方块核做卷积，也就是低通滤波器——它在做模糊</b>。", "body")])
    add([P("常见的降采样方式，性质完全不同：", "body")])
    add(TBL(["方式", "做什么", "对高频信息"], [
        ["平均池化", "窗口内取均值", "抹平——低通滤波"],
        ["最大池化", "窗口内取最大值", "保留局部峰值"],
        ["带步长的卷积", "让网络自己学该保留什么", "由训练决定"],
    ], widths=[WIDTH * 0.22, WIDTH * 0.42, WIDTH * 0.36]))
    add([P(
        "这个区别对深度伪造检测特别关键。<b>融合边界、压缩不一致、重打光痕迹，"
        "这些证据都在高频段</b>——正是平均池化滤掉的那部分。所以这个领域有一批工作"
        "专门加频域分支或高通滤波器（比如 F3-Net、SRM 滤波器），方向跟平均池化相反。", "body")])
    add([P(
        "你的模型连做三次 4×4 平均池化（窗口 4 大于步长 2，窗口还是重叠的），"
        "256×256 变成 22×22，<b>空间面积压掉 136 倍</b>。融合边界在原图上只有几个像素宽，"
        "三轮平均之后基本不剩什么。", "body")])

    one(P("2.4　归一化：BatchNorm 到底在算什么", "h2"))
    add([P(
        "深层网络训练时有个麻烦：每一层的输入分布会随着前面层的参数更新而漂移，"
        "导致后面的层要不断适应。批归一化（Batch Normalization，BN）的做法是"
        "<b>把每一层的输出强行拉回到均值 0、方差 1</b>，再用两个可学习参数缩放平移回去。", "body")])
    add([P(
        "关键在于「均值和方差是在哪些元素上算的」。对卷积层的 BN，统计量是"
        "<b>按通道</b>计算的，每个通道在 <span>N × T × H × W</span> 这些维度上求均值和方差。"
        "注意<b>不是只按批维度</b>——这是一个常见误解。", "body")])

    one(P("2.5　batch = 1 意味着什么", "h3"))
    add([P(
        "你的配置是 <span>physical_batch_size = 1</span>，也就是每次优化步只处理一个片段。"
        "由于 BN 是在 N×T×H×W 上统计的，batch=1 时仍然有不少元素参与：", "body")])
    add(TBL(["归一化层", "张量形状", "每通道参与统计的元素数"], [
        ["norm1", "16 × 8 × 125 × 125", "125,000"],
        ["norm2", "24 × 8 × 59 × 59", "27,848"],
        ["norm3", "32 × 8 × 26 × 26", "5,408"],
        ["<b>最后的 BatchNorm2d</b>", "<b>32 × 22 × 22</b>", "<b>484</b>"],
    ], widths=[WIDTH * 0.34, WIDTH * 0.34, WIDTH * 0.32]))
    add([P(
        "所以前三层大概率没问题。<b>风险集中在最后那个 BatchNorm2d</b>——只有 484 个元素，"
        "而且全部来自同一条视频的同一张特征图。它的归一化完全由这一条视频自己决定，"
        "相当于对每个样本做了一次自适应对比度归一化。而这一层的输出正是喂给分类头的那 15488 维特征。", "body")])
    add([P(
        "文献上的证据：Wu 与 He 的 Group Normalization（ECCV 2018）在 ResNet-50 / ImageNet 上测得，"
        "batch 降到 2 时 BN 的错误率是 34.7%，而不依赖批统计量的 GN 是 24.1%，"
        "差 10.6 个百分点。他们明确指出原因是<b>批统计量估计不准</b>。", "body")])
    add([P(
        "但也要说另一面：Masters 与 Luschi（2018）发现<b>小批量本身对泛化不是坏事</b>，"
        "他们测试的最优区间是 2 到 32。所以问题不是「batch 小」，而是<b>「batch=1 配 BatchNorm」</b>这个组合。"
        "标准做法是批量很小时改用 GroupNorm 或 InstanceNorm——它们不依赖批维度。", "body")])

    one(P("2.6　激活函数：为什么需要非线性", "h2"))
    add([P(
        "如果只有卷积和池化，整个网络就是一串线性变换的复合，而线性变换的复合仍然是线性变换——"
        "堆一百层和堆一层的表达能力完全一样。<b>激活函数提供非线性</b>，这是深层网络有意义的前提。", "body")])
    add([P(
        "常见的两个：<span>sigmoid</span> 把输入压到 (0,1)，<span>ReLU</span> 把负数变成 0、正数保持不变。"
        "sigmoid 有个致命问题——当输入的绝对值大于 4 时，它的导数小于 0.018，"
        "梯度传不回去，这叫<b>饱和</b>。", "body")])
    add([P(
        "你的项目踩过这个坑：原论文用 sigmoid，而实测发现预激活的幅度从第一段的 0.35 漂移到第三段的 8.4，"
        "<b>91% 的值越过了 |x| &gt; 4</b>。三个 sigmoid 叠起来，传到第一层卷积的梯度上限是 0.25³，"
        "单步更新量测出来是 10⁻⁷ 量级——等于没在训练。改成 ReLU 加 BatchNorm 之后才动起来。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 3
    one(P("3　训练：模型是怎么「学」的", "h1"))
    add(ANCHOR(
        "你问过：「阶段 B 其实没有反向传播调参吧？」和「相当于每个实验更新了 50 次吗？」。"
        "第一个问题的答案是「有，但只更新头」；第二个是「不是 50 次，是一万七千次」。"
        "这一章把这两件事讲清楚。"))

    one(P("3.1　损失函数：把「错得多离谱」变成一个数", "h2"))
    add([P(
        "训练需要一个可优化的目标。二分类用<b>二元交叉熵</b>（BCE）：模型对每个样本输出一个"
        "概率 p（认为它是伪造的概率），真实标签是 y（0 或 1），损失是", "body")])
    add([CODE(["L = -[ y * log(p) + (1-y) * log(1-p) ]"])])
    add([P(
        "直觉是：模型给正确答案的概率越高，损失越小；<b>如果模型很自信地答错了，"
        "损失会趋于无穷大</b>。这就是为什么损失可以大于 1 甚至大于 2。", "body")])

    one(P("3.2　ln 2 是从哪来的", "h3"))
    add([P(
        "如果模型什么都没学到，对所有输入都输出 p = 0.5，那么无论标签是 0 还是 1，"
        "损失都是 −log(0.5) = <b>log 2 ≈ 0.6931</b>。所以在类别平衡的数据上，"
        "<b>ln 2 是「完全没学到东西」的损失水平</b>——相当于 AUROC 图上的 0.50 那条线。", "body")])
    add(BUL([
        "训练损失<b>低于</b> ln 2 → 模型在训练数据上确实拟合到了内容",
        "训练损失<b>高于</b> ln 2 → 模型还不如恒定输出 0.5",
    ]))
    add([P(
        "你的三次运行：实验一 60 轮里有 46 轮低于 ln 2；实验三降到 0.5618；"
        "<b>实验二 60 轮里 0 轮低于 ln 2</b>，最低 1.0524。第三个是明确的「优化没有启动」，"
        "跟前两个的失败性质完全不同。", "body")])

    one(P("3.3　梯度下降：往哪个方向调参数", "h2"))
    add([P(
        "损失是所有参数的函数。<b>梯度</b>是损失对每个参数的偏导数，它指向「损失上升最快」的方向。"
        "所以往梯度的反方向走一小步，损失就会下降。步子的大小叫<b>学习率</b>。", "body")])
    add([CODE(["新参数 = 旧参数 - 学习率 x 梯度"])])
    add([P(
        "<b>反向传播</b>不是一种学习算法，它只是「高效计算梯度」的方法——"
        "利用链式法则从输出层往回逐层算，避免重复计算。它和梯度下降是两件事："
        "反向传播算梯度，梯度下降用梯度更新参数。", "body")])

    one(P("3.4　优化器：SGD 和 Adam", "h3"))
    add(BUL([
        "<b>SGD</b>：严格按上面那个公式走。简单、稳定，但对学习率敏感，收敛慢。",
        "<b>Adam</b>：为每个参数维护一个自适应的步长（根据历史梯度的一阶和二阶矩估计），"
        "通常收敛更快，对学习率不那么敏感。",
    ]))
    add([P(
        "你的项目里阶段 A 用 Adam，阶段 B 用 SGD（后者是为了复现原论文）。"
        "两个阶段都用了<b>指数衰减</b>：每个 epoch 学习率乘以 γ = 0.95，"
        "60 轮之后学习率降到初始值的 4.6%。前期大步快速接近，后期小步精细调整。", "body")])

    one(P("3.5　batch、step、epoch：三个容易混的概念", "h2"))
    add(TBL(["概念", "定义", "你的项目里的值"], [
        ["batch（批）", "一次优化步处理多少个样本", "<b>1</b> 个片段"],
        ["step（步）", "一次参数更新。处理完一个 batch 就更新一次", "阶段 A 每轮 2000 步"],
        ["epoch（轮）", "把这一轮要用的样本全部过一遍", "阶段 A 上限 60，阶段 B 上限 50"],
    ], widths=[WIDTH * 0.2, WIDTH * 0.5, WIDTH * 0.3]))
    add([P("三者的关系：", "body")])
    add([CODE([
        "每轮步数 = 每轮样本数 / batch 大小",
        "总更新次数 = 每轮步数 x 轮数",
        "",
        "阶段 A：每类每轮采样 1000 个片段，两类共 2000 个，batch=1",
        "        -> 每轮 2000 步，60 轮 = 120,000 次参数更新",
        "",
        "阶段 B：只用真实视频，每轮 1000 个片段，batch=1",
        "        -> 每轮 1000 步，17 轮 = 17,000 次参数更新",
    ])])
    add([P(
        "<b>所以「50」是 epoch 的上限，不是更新次数。</b>实际的更新次数在一万到十二万这个量级。"
        "而且两次阶段 B 都没跑满 50 轮——它们触发了<b>早停</b>：连续若干轮验证指标没有提升就停止，"
        "避免继续训练只是在过拟合。", "body")])

    one(P("3.6　冻结：阶段 B 到底在更新什么", "h2"))
    add([P("阶段 B 确实有反向传播，但梯度只流到头，不流进卷积层。冻结提取器的代码做两件事：", "body")])
    add([CODE([
        "for parameter in extractor.parameters():",
        "    parameter.requires_grad_(False)   # 不再计算和累积梯度",
        "extractor.eval()                      # 冻住 BatchNorm 的滑动统计量",
    ])])
    add([P(
        "<b>第二行同样重要。</b>BatchNorm 在训练模式下会持续更新它的滑动统计量。"
        "阶段 B 只喂真实视频，如果不调 <span>eval()</span>，这些统计量会慢慢漂移到"
        "「只见过真实视频」的分布上——等于变相训练了提取器，冻结就形同虚设。", "body")])
    add([P(
        "至于更新的是什么：贝叶斯头不是学一组固定的权重，而是学每个权重的<b>分布</b>（均值场近似，"
        "即假设每个权重独立服从一个高斯分布）。所以每个权重对应两个待优化的参数：均值和标准差。"
        "头本身有 7,930,881 个权重，<b>阶段 B 实际在优化约 1586 万个变分参数</b>。", "body")])
    add([P(
        "这也是项目早期那个 KL 问题的来源：目标函数里的 KL 散度项衡量的是"
        "「学到的分布」和「先验分布」的距离，而它是<b>对每个参数求和</b>的——"
        "1586 万个变分参数各贡献一份。下一节把这件事完整讲清楚。", "body")])


    one(P("3.7\u3000KL 散度：贝叶斯头的目标函数里那一项", "h2"))
    add(ANCHOR(
        "你问过：「KL 散度是什么？为什么占了目标函数 95% 就等于分类信号被淹没？」"
        "第二个问题的答案是——<b>不等于</b>。这一节把原理讲清楚，并且用你自己运行的诊断数据"
        "说明实际发生的是另一回事。"))

    one(P("3.7.1\u3000KL 散度衡量两个分布有多不一样", "h3"))
    add([P(
        "普通网络每个权重学一个数；贝叶斯网络每个权重学一个<b>分布</b>。因为一个数没法表达"
        "「我不确定」，而分布可以——标准差小表示确定，大表示模型自己也拿不准。", "body")])
    add([P(
        "理想中你想要真实后验 p(w | 数据)，但它算不出来。于是退而求其次：用一个简单的分布 q(w) "
        "去近似它。这套做法叫<b>变分推断</b>，「均值场」是指假设所有权重相互独立、各自一个高斯。", "body")])
    add([P("衡量 q 和 p 差多远的量就是 <b>KL 散度</b>：", "body")])
    add([CODE([
        "KL(q || p) = 积分  q(x) * log[ q(x) / p(x) ] dx",
        "",
        "性质： 恒 >= 0，且仅当 q = p 时为 0",
        "       不对称，KL(q||p) != KL(p||q)",
        "       两个高斯之间有闭式解，不需要数值积分：",
        "",
        "KL( N(mu, sq^2) || N(0, sp^2) )",
        "     = log(sp/sq) + (sq^2 + mu^2) / (2*sp^2) - 1/2",
    ])])
    add([P(
        "你的项目里 q 是学到的 N(μ, σ²)，p 是<b>先验</b> N(0, 0.1²)——「先验」就是训练之前"
        "你假设权重应该长什么样：都在 0 附近、尺度 0.1。", "body")])

    one(P("3.7.2\u3000ELBO：目标函数为什么长这样", "h3"))
    add([P("你想最大化数据的证据 log p(D)。它可以精确分解成两块：", "body")])
    add([CODE([
        "log p(D)  =  ELBO  +  KL( q(w) || 真实后验 )",
    ])])
    add([P(
        "因为 KL 恒非负，所以 <b>ELBO ≤ log p(D)</b>——这就是「证据下界」"
        "（Evidence Lower BOund）这个名字的来源。最大化 ELBO 同时做两件事：抬高证据，"
        "并让 q 逼近真实后验。而 ELBO 自己又分解成两项：", "body")])
    add([CODE([
        "ELBO  =  E_q[ log p(D|w) ]     -     KL( q(w) || p(w) )",
        "         ^^^^^^^^^^^^^^^^^           ^^^^^^^^^^^^^^^^^^",
        "         似然项：拟合数据            正则项：别离先验太远",
    ])])
    add([P(
        "实际最小化的是 −ELBO，也就是<b>负对数似然加 KL</b>。"
        "<b>分类信号在第一项，KL 在第二项</b>——这就是你问的那两项的来历。", "body")])

    one(P("3.7.3\u3000为什么参数量决定 KL 的大小", "h3"))
    add(TBL(["", "是对什么求和", "你的规模"], [
        ["似然项", "<b>数据点</b>", "batch = 1，<b>一个样本</b>"],
        ["KL 项", "<b>参数</b>", "<b>7,930,881 个权重</b>"],
    ], widths=[WIDTH * 0.16, WIDTH * 0.34, WIDTH * 0.5]))
    add([P(
        "均值场假设下 KL 可以拆开：KL(q‖p) = Σᵢ KL(qᵢ‖pᵢ)，<b>每个权重独立贡献一份</b>。"
        "所以参数越多 KL 越大，而似然项不随参数量变化。<b>这个失衡是结构性的。</b>", "body")])

    one(P("3.7.4\u3000算你的实际数字", "h3"))
    add([P(
        "代入你的配置（rho_init = −5.0，所以 σq = softplus(−5) = 0.006716；先验 σp = 0.1）：", "body")])
    add([CODE([
        "单个权重的 KL      =  2.2040",
        "头的权重总数       =  7,930,881",
        "未缩放的 KL 总量   =  17,479,322        <- 一千七百万",
        "",
        "代码里的缩放 = kl_weight / num_train_clips = 1e-3 / 1000 = 1e-6",
        "缩放后的 KL       =  17.48",
        "",
        "实测第 1 轮总损失  =  18.5938",
        "        -> KL 占 94.0%，似然占 1.11",
    ])])
    add([P(
        "跟项目记录里的「95–99.9%」对得上。如果不加 kl_weight = 10⁻³，"
        "KL 会是 17,479，占比 99.994%。<b>这不是估计，是闭式解算出来再跟实测损失对账。</b>", "body")])

    one(P("3.7.5\u3000但「占 94%」不等于「淹没」", "h3"))
    add([P(
        "<b>这是这一节最重要的一句话，也是这份文档初稿写错的地方。</b>"
        "损失里的占比和梯度里的话语权是两回事。", "body")])
    add([P(
        "极端反例：给损失函数加一个常数 1000000，它会占 99.99% 的损失<b>值</b>，"
        "但它的<b>梯度是 0</b>，对训练毫无影响。参数只跟着梯度走，不跟着损失值走。", "body")])
    add([P(
        "所以「KL 占 94%」本身<b>证明不了</b>任何事。要看的是 KL 的梯度有多大。", "body")])

    one(P("3.7.6\u3000实测：σ 从头到尾没动过", "h3"))
    add([P("阶段 B 每一轮都记录了后验诊断。实验三的记录是：", "body")])
    add([CODE([
        "fc1_weight_sigma_mean    第 1 轮 0.006716  ->  最后一轮 0.006716",
        "fc1_weight_rho_mean      第 1 轮 -5.000000 ->  最后一轮 -5.000000",
    ])])
    add([P(
        "<b>完全没动。</b>0.006716 正是初始化值。算一下梯度就知道为什么：", "body")])
    add([CODE([
        "dKL/d(sigma)          = -1/sq + sq/sp^2  =  -148.2",
        "dKL/d(rho)  缩放前     = -0.992",
        "dKL/d(rho)  缩放后     = -9.92e-07          <- 乘上 1e-6",
        "单步变化 = lr x 梯度   = 1e-4 x 9.92e-7 = 9.92e-11",
        "17000 步累计           = 1.69e-06",
    ])])
    add([P(
        "<b>预测累计变化 1.7×10⁻⁶，实测变化 0.000000（六位小数）。完全吻合。</b>", "body")])
    add([P("所以实验三阶段 B 的真实情况是：", "body")])
    add(BUL([
        "KL 确实占了 94% 的损失<b>值</b>——这是算术事实",
        "<b>但它的梯度小到不产生任何实际影响</b>，kl_weight = 10⁻³ 把它压得太狠了",
        "σ 全程冻在初始值，<b>贝叶斯头的不确定性参数从来没有被学习过</b>",
        "μ 倒是动了（验证 AUROC 从 0.5975 升到 0.6569），所以头学到了东西——"
        "<b>但学的是一个 σ 固定的确定性头，外面套了个贝叶斯的壳</b>",
    ]))

    one(P("3.7.7\u3000这意味着什么", "h3"))
    add([P(
        "<b>对论文不影响</b>：论文没有对不确定性估计做任何声称，报告的是 AUROC。", "body")])
    add([P(
        "<b>但有一条底线</b>：如果将来要说「贝叶斯头提供了校准的不确定性估计」，"
        "这个说法在当前实现下站不住。测试报告里的 mean_predictive_std = 0.2086 "
        "是用一个从未被拟合过的 σ 采样出来的，它反映的是初始化值，不是学到的不确定性。", "body")])
    add([P(
        "<b>方法上的教训</b>：诊断一个训练问题时，先问「这一项的梯度有多大」，"
        "而不是「这一项在损失里占多少」。第二个问题容易回答，但它回答的不是你想问的事。"
        "这个项目里另外几次判断——学习率、池化、容量——也都该用同样的标准复核。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 4
    one(P("4　评估：一个数字什么时候可信", "h1"))
    add(ANCHOR(
        "你问过：「留出测试是啥？」「不是有验证集和测试集了吗，封存测试集是为了什么？」"
        "这一章是整份文档里对你论文最重要的一章——你这篇论文讲的就是这件事。"))

    one(P("4.1　为什么是三个集合不是两个", "h2"))
    add([P("一句话概括：<b>任何被你用来做决定的数据，都不再是无偏的估计。</b>", "body")])
    add(TBL(["集合", "你在它上面拟合了什么", "读数是否乐观偏高"], [
        ["训练集", "模型参数", "严重偏高"],
        ["验证集", "<b>选择</b>：哪一轮、哪个学习率、哪个结构", "<b>也偏高</b>，只是轻一些"],
        ["测试集（留出）", "什么都没有", "无偏"],
    ], widths=[WIDTH * 0.22, WIDTH * 0.48, WIDTH * 0.3]))
    add([P(
        "验证集的偏差常被低估。模型确实没有「学习」验证集，但你<b>用它做了选择</b>——"
        "从 60 轮里挑出验证指标最高的那一轮。这个挑选动作本身就是一种拟合。", "body")])

    one(P("4.2　选择偏差：用你自己的数据看", "h3"))
    add([P(
        "实验二的阶段 A 在 DFD 的 62 条视频验证集上跑了 60 轮：", "body")])
    add([CODE([
        "最高      0.8654  （出现在第 2 轮）",
        "中位数    0.3451",
        "均值      0.3726",
        "60 轮里高于 0.5 的只有 5 轮",
        "训练交叉熵 60 轮里 0 轮低于 ln 2  -> 模型什么都没学到",
    ])])
    add([P(
        "<b>最高的那一轮出现在第 2 轮</b>，那时候网络刚训练了两轮、训练损失还在 1.21，"
        "确凿地什么都没学到。但如果按「取验证 AUROC 最高的一轮」这个规则挑 checkpoint，"
        "你会挑中它，然后报告 0.8654。", "body")])
    add([P(
        "<b>这个 0.8654 不是模型的能力，是 62 条视频的验证集在 60 次含噪读数里能产生的最大值。</b>"
        "多跑几轮，这个「最好」还会更高，而模型没有任何改善。测试集存在的唯一目的就是逃开这件事。", "body")])

    one(P("4.3　混淆矩阵与四个基本量", "h2"))
    add([P("给定一个判定阈值，每个样本落入四格之一：", "body")])
    add([CODE([
        "                预测为真实      预测为伪造",
        "  实际真实         TN              FP        <- 假阳性：把真的当成假的",
        "  实际伪造         FN              TP        <- 真阳性：正确抓出伪造",
        "",
        "  真阳率 TPR = TP / (TP + FN)     抓出了多少伪造",
        "  假阳率 FPR = FP / (FP + TN)     冤枉了多少真实",
    ])])

    one(P("4.4　ROC 与 AUROC", "h2"))
    add([P(
        "阈值是可调的：调低就抓得多但冤枉得也多。把阈值从最松扫到最紧，"
        "每个阈值得到一对 (FPR, TPR)，连起来就是 <b>ROC 曲线</b>。"
        "曲线下的面积就是 <b>AUROC</b>。", "body")])
    add([P(
        "AUROC 有一个更直观的等价定义：<b>随机抽一个伪造样本和一个真实样本，"
        "模型给伪造样本打分更高的概率</b>。所以：", "body")])
    add(BUL([
        "AUROC = 0.5 → 完全随机，等于抛硬币",
        "AUROC = 1.0 → 完美排序",
        "AUROC = 0.3 → 系统性地<b>反着排</b>。注意它和 0.7 一样「有信息量」，只是方向相反",
    ]))
    add([P(
        "最后一条在你的项目里出现过两次：DFD 的验证 AUROC 长期在 0.35 附近，"
        "以及码率这个元数据的 AUROC 是 0.2174——把方向反过来读，它的判别力等于 0.7826。", "body")])

    one(P("4.5　为什么 Accuracy 和 AP 在你的数据上不能用", "h2"))
    add([P(
        "<b>AUROC 对类别比例免疫</b>，因为它只看排序，不看两类各有多少个。"
        "但 Accuracy 和 Average Precision 不是。", "body")])
    add([P(
        "CelebDF++ 测试集是 169 条真实、5431 条伪造，比例约 1:32。"
        "一个「把所有输入都判为伪造」的平凡模型，Accuracy 就有 <b>0.9698</b>。"
        "你的模型 AP 是 0.9733——看着惊艳，实际只比这个基准率高 0.0035。", "body")])
    add([P(
        "<b>报告 AP 时必须同时给出基准率</b>，否则这个数字没有信息量。这是审稿人会抓的点。", "body")])

    one(P("4.6　EER 和 TPR@5%FPR", "h3"))
    add(BUL([
        "<b>EER</b>（等错误率）：调整阈值使假阳率等于假阴率时的那个错误率。随机水平是 0.50。",
        "<b>TPR@5%FPR</b>：把假阳率固定在 5%（即只允许冤枉 5% 的真实视频），"
        "此时能抓出多少比例的伪造。这个指标贴近实际部署——现实中你不能大量误伤真实内容。"
        "随机水平是 0.05。",
    ]))

    one(P("4.7　置信区间：一个点估计不够", "h2"))
    add([P(
        "AUROC = 0.5284 只是一个点。换一批测试视频会得到不同的值。"
        "<b>自助法（bootstrap）</b>用来估计这个波动：从测试集里有放回地重复抽样，"
        "每次算一个 AUROC，抽两千次，取结果的 2.5% 和 97.5% 分位数作为 95% 区间。", "body")])
    add([P(
        "<b>关键是「抽什么」。</b>如果按单条视频抽，就等于假设每条视频是独立观测。"
        "但在 CelebDF++ 上，一条真实片段平均衍生 84 条伪造视频，它们共享背景、服装、光照、"
        "镜头运动——把它们当独立样本会低估不确定性。按<b>身份</b>抽样才把相关的样本作为整体处理。", "body")])
    add([P(
        "你的项目实测发现，这两种抽样单位在同一份分数上给出不同宽度的区间，"
        "而且在 CelebDF++ 上对「是否高于随机」给出了相反的判断。"
        "所以论文里必须<b>明确声明重采样单位</b>——这本身就是一个可报告的发现。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 5
    one(P("5　表征：特征好不好，怎么测", "h1"))
    add(ANCHOR(
        "你问过：「特征表征测量这个实验是干什么的？t-SNE 是什么？」"
        "以及「这个探针不是随机初始化的吗，怎么就不好了？」"))

    one(P("5.1　什么是「特征」", "h2"))
    add([P(
        "特征提取器把一个视频片段变成一个 15488 维的向量。这个向量就是模型对这段视频的<b>表征</b>——"
        "它是模型「看到」的东西的数值形式。理想情况下，真实视频的向量应该聚在空间的一个区域，"
        "伪造视频聚在另一个区域。", "body")])
    add([P(
        "阶段 A 交付给阶段 B 的就是这个提取器（不是分类器，分类头训练完就丢了）。"
        "所以在进阶段 B 之前必须先回答：<b>这个提取器学到东西了吗？</b>", "body")])

    one(P("5.2　线性探针", "h2"))
    add([P(
        "做法很直接：冻住提取器，把视频喂进去拿到特征向量，然后训练一个<b>线性分类器</b>"
        "（岭回归就够）去区分真假，报告它在<b>没见过的身份</b>上的 AUROC。", "body")])
    add([P(
        "为什么用线性分类器而不是更强的模型？因为要测的是「特征本身好不好」，"
        "而不是「后面接的分类器强不强」。如果特征里的信息需要一个复杂的非线性模型才能提取出来，"
        "那对你的场景没意义——<b>你的分类头两层之间没有激活函数，数学上就是一个线性泛函</b>。", "body")])
    add([P(
        "这个方法有名字，叫 linear probing，是表征学习里的标准评估协议。"
        "术语出自 Alain 与 Bengio（2016）；SimCLR、MoCo、DINO、CLIP 这些自监督工作"
        "都把「linear evaluation protocol」当作头条指标。", "body")])

    one(P("5.3　随机初始化基线：为什么参照线不是 0.5", "h2"))
    add([P(
        "这是整个测量里最容易忽略、也最关键的一步。", "body")])
    add([P(
        "一个<b>完全没训练过</b>的卷积网络，它的输出不是纯噪声。随机卷积加池化会编码"
        "低层图像统计量——分辨率、压缩强度、色彩分布、纹理密度。而这些东西在数据集里"
        "本身就可能跟真假相关。", "body")])
    add([P("所以随机网络的探针值不是 0.5。你的实测：", "body")])
    add(TBL(["提取器", "随机初始化", "训练后", "差值"], [
        ["CelebDF++ 人脸裁剪 · 早期划分", "0.5778", "0.7322", "+0.1544"],
        ["CelebDF++ 人脸裁剪 · 实验三", "0.7552", "0.7149", "−0.0402"],
        ["DFD 整帧 · 实验一", "0.4900", "0.5033", "+0.0133"],
    ], widths=[WIDTH * 0.44, WIDTH * 0.19, WIDTH * 0.19, WIDTH * 0.18]))
    add([P(
        "<b>判断标准因此不是「探针 &gt; 0.5」，而是「训练后的值有没有超出同一架构的随机网络能给出的范围」。</b>"
        "如果没有超出，说明训练没有带来可测的增益——特征里的可分性可能本来就在那儿。", "body")])
    add([P(
        "这也是你当初困惑的点：探针读出 0.7149 看着不错，但同架构的随机网络读出 0.7552。"
        "训练之后反而更低。（这一行只抽了一次随机样本，所以还不能下结论，但方向值得警惕。）", "body")])

    one(P("5.4　t-SNE：能看，不能算", "h2"))
    add([P(
        "t-SNE 是一种降维可视化方法（van der Maaten 与 Hinton，2008）。"
        "它把高维向量映射到平面上的点，规则是<b>让原本相近的向量在平面上也相近</b>。", "body")])
    add([P("怎么读：如果两类聚成两团，说明特征里有可分结构；如果混在一起，说明没有。", "body")])
    add([P(
        "但 t-SNE 有个必须知道的局限：<b>它会制造也会抹掉结构</b>。"
        "簇之间的距离、簇的大小、点的密度都不能按字面理解，换一个随机种子或者 perplexity 参数，"
        "图会变。所以它只能用来<b>看</b>，结论必须靠定量指标背书。", "body")])
    add([P(
        "你的项目里配的定量指标是「在这个平面上按身份留一做 5-NN 分类」，读数是 0.375——"
        "比随机（0.5）还低。这个数才是可引用的，图只是辅助。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 6
    one(P("6　迁移学习与预训练", "h1"))
    add(ANCHOR(
        "你问过：「怎么用 ImageNet 和 Kinetics 预训练？Leyva 那个实验用了吗？」"
        "答案是：他没用，而这很可能是你这个项目最大的结构性问题。"))

    one(P("6.1　从零训练为什么难", "h2"))
    add([P(
        "神经网络的参数是随机初始化的。要让它学会「什么是边缘、什么是纹理、什么是人脸」，"
        "需要大量数据。而这些低层视觉概念<b>对所有视觉任务都是通用的</b>——"
        "识别猫和识别伪造人脸，前几层要提取的东西高度相似。", "body")])
    add([P(
        "所以从零开始训练是在重复造轮子，而且是用一个远小于 ImageNet 的数据集造。"
        "你的 DFD 训练集只有 20 个身份、2158 条视频。", "body")])

    one(P("6.2　预训练的范式", "h2"))
    add([P(
        "<b>预训练</b>就是先在一个大数据集上把网络训好，再把这些权重当作新任务的起点。"
        "两个最常用的来源：", "body")])
    add(BUL([
        "<b>ImageNet</b>：128 万张图、1000 类。图像任务的标准起点。",
        "<b>Kinetics-400</b>：约 30 万段视频、400 类动作。视频任务的标准起点。",
    ]))
    add([P("拿到预训练权重之后有两种用法：", "body")])
    add(TBL(["用法", "做什么", "适用场景"], [
        ["<b>微调</b>（fine-tuning）", "载入权重后继续训练全部或部分层", "新任务数据较多"],
        ["<b>冻结特征</b>", "完全冻住主干，只训练新加的头", "新任务数据很少"],
    ], widths=[WIDTH * 0.26, WIDTH * 0.44, WIDTH * 0.3]))

    one(P("6.3　具体怎么用", "h3"))
    add([P("PyTorch 自带 Kinetics-400 预训练的 3D 主干，你的环境里就有：", "body")])
    add([CODE([
        "from torchvision.models.video import r2plus1d_18, R2Plus1D_18_Weights",
        "import torch",
        "",
        "extractor = r2plus1d_18(weights=R2Plus1D_18_Weights.KINETICS400_V1)",
        "extractor.fc = torch.nn.Identity()    # 去掉分类层，只要特征",
        "",
        "# 输入 [N, C, T, H, W]，正好是你的 loader 已经产出的形状",
        "# 输出 512 维特征",
    ])])
    add([P("换上去之后，参数分配会完全翻转：", "body")])
    add(TBL(["", "特征提取器", "分类头"], [
        ["你现在的模型", "90,280（随机初始化）", "7,930,881"],
        ["R(2+1)D-18", "<b>31,505,325（Kinetics 预训练）</b>", "<b>约 263,000</b>"],
    ], widths=[WIDTH * 0.28, WIDTH * 0.42, WIDTH * 0.3]))
    add([P(
        "注意这个翻转的附带好处：<b>头的变分参数从 1586 万降到约 53 万，KL 项自动小 30 倍</b>——"
        "你当初靠 <span>kl_weight = 1e-3</span> 硬压下去的那个问题，换个主干基本就不存在了。", "body")])

    one(P("6.4　膨胀（inflation）：一个你用了形式但没拿到好处的技巧", "h2"))
    add([P(
        "你的架构被描述成「沿时间轴膨胀为 3D」。<b>「膨胀」这个操作是 I3D 提出的</b>"
        "（Carreira 与 Zisserman，CVPR 2017），做法是把一个 2D 卷积核沿时间轴复制若干份，"
        "得到一个 3D 卷积核。", "body")])
    add([P(
        "而它存在的<b>唯一目的</b>就是——让 3D 网络能够继承 2D 网络的 ImageNet 预训练权重。"
        "论文标题里的 I 就是 Inflated。", "body")])
    add([P(
        "<b>你做了膨胀这个动作，但从随机权重开始。</b>等于用了这个技术的形式，"
        "没拿到它设计出来要给的那个好处。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 7
    one(P("7　数据、泄漏与捷径", "h1"))
    add(ANCHOR(
        "这一章的内容你在项目里已经摸得很透了——供体泄漏那条线索是你自己挖出来的，"
        "也是你论文最强的贡献点。这里做的是归位：把你的具体发现放回它所属的通用框架里。"))

    one(P("7.1　划分的单位", "h2"))
    add([P(
        "机器学习最基本的纪律是：<b>测试数据不能以任何形式出现在训练里</b>。"
        "难点在于「以任何形式」的边界在哪。", "body")])
    add(TBL(["划分单位", "保证什么", "漏了什么"], [
        ["按视频", "同一条视频不跨集合", "同一个人的<b>其他</b>视频可能在训练里"],
        ["按目标身份", "被操纵的那个人不跨集合", "<b>提供人脸的那个人</b>可能在训练里"],
        ["按目标 + 供体", "两个身份都不跨集合", "—"],
    ], widths=[WIDTH * 0.22, WIDTH * 0.36, WIDTH * 0.42]))

    one(P("7.2　为什么身份泄漏会抬高读数", "h3"))
    add([P(
        "检测器可能学到的不是「伪造痕迹」，而是「这个人长什么样」。"
        "Dong 等人（CVPR 2023）的工作表明，二分类的深伪检测器会编码身份相关的信息，"
        "损害跨域泛化——他们称之为隐式身份泄漏。", "body")])
    add([P(
        "如果训练时见过某个人的真实视频，测试时又遇到用这个人的脸做的伪造视频，"
        "模型可以靠「认出这张脸」而不是「发现伪造痕迹」来做判断。这样得到的高分不代表泛化能力。", "body")])

    one(P("7.3　你发现的那件事", "h2"))
    add([P(
        "一条换脸视频涉及<b>两个</b>身份：被替换的<b>目标</b>和提供人脸的<b>供体</b>。"
        "只隔离目标身份，供体仍然可以跨集合。你的实测：", "body")])
    add(TBL(["", "target-only 测试集伪造数", "其中有供体", "供体在训练集"], [
        ["DFD", "406", "406（100%）", "<b>313（77.1%）</b>"],
        ["CelebDF++", "10,255", "5,975（58.3%）", "<b>3,879（64.9%）</b>"],
    ], widths=[WIDTH * 0.2, WIDTH * 0.28, WIDTH * 0.24, WIDTH * 0.28]))
    add([P(
        "注意 CelebDF++ 那个 58.3%：<b>不是所有伪造方法都有供体</b>。"
        "语音驱动的说话人生成只用一个身份加一段音频，没有第二张脸，供体约束对它不适用。"
        "所以百分比必须说清楚分母——这是个容易出错的地方，你的论文里已经处理对了。", "body")])

    one(P("7.4　捷径学习", "h2"))
    add([P(
        "更一般的问题叫<b>捷径学习</b>（shortcut learning，Geirhos 等人，2020）："
        "模型找到一个在训练数据上有效、但不是任务本意的判别依据。", "body")])
    add([P("深伪检测里常见的捷径：", "body")])
    add(BUL([
        "<b>分辨率与长宽比</b>：某些生成方法固定输出 256×256 的正方形，而真实视频是宽屏",
        "<b>压缩强度</b>：伪造视频经过额外的编码环节，码率和每像素比特数与真实视频不同",
        "<b>时长与帧数</b>：生成流程往往截取固定长度",
        "<b>人脸框尺寸</b>：如果两类的人脸大小分布不同，裁剪后的重采样倍数也不同",
    ]))
    add([P(
        "<b>检验方法叫元数据基线</b>：不解码任何像素，只用容器里的一个标量（比如文件码率）"
        "给视频排序，把这个排序当作检测器来打分。如果它就能把两类分开，"
        "说明数据集本身存在与标签相关的捷径。", "body")])
    add([P(
        "你的实测：CelebDF++ 测试集上，<b>文件码率单独的方向调整后 AUROC 约 0.78</b>，"
        "而你训练了一整套模型的读数是 0.5284。同一测量在 DFD 上是 0.5243，接近随机——"
        "所以这是 CelebDF++ 特有的问题，不是通病。", "body")])
    add([P(
        "要补充一点严谨性：你还测了模型分数与码率的秩相关，结果接近零（−0.02）。"
        "这说明<b>模型没有表现出与码率的单调关联</b>——但秩相关只能排除单调关系，"
        "不能排除非线性依赖。所以正确的说法是「未观察到单调关联」，不是「模型没有使用码率」。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 8
    one(P("8　视频比图像多出来的东西", "h1"))

    one(P("8.1　时序建模的几条路线", "h2"))
    add(TBL(["路线", "做法", "代表工作"], [
        ["2D + 聚合", "逐帧用 2D 网络打分，最后平均", "早期方法，简单但丢时序"],
        ["<b>3D 卷积</b>", "卷积核同时跨越空间和时间", "C3D、I3D、R(2+1)D（<b>你的模型属于这一类</b>）"],
        ["双流", "一路 RGB，一路光流，分别建模外观和运动", "Two-Stream 系列"],
        ["循环网络", "2D 提特征，LSTM/GRU 建模帧间关系", "早期深伪视频检测常用"],
        ["Transformer", "自注意力建模长程时序依赖", "近年主流，如 TimeSformer、VideoMAE"],
    ], widths=[WIDTH * 0.16, WIDTH * 0.42, WIDTH * 0.42]))

    one(P("8.2　片段采样", "h2"))
    add([P(
        "视频长度不一，而网络需要固定长度的输入。所以从视频里截取<b>片段</b>（clip）。"
        "你的配置是 8 帧。训练时每条视频随机取一个片段（相当于数据增强），"
        "评估时取 8 个等距片段再把分数平均——这样读数更稳定，也覆盖整段视频。", "body")])

    one(P("8.3　视频级聚合", "h2"))
    add([P(
        "从「片段分数」到「视频分数」需要聚合。最常见的是取平均，也有取最大（只要有一段可疑就判可疑）。"
        "<b>这个选择必须在论文里说明</b>，因为它直接影响指标——而且「帧级 AUROC」和「视频级 AUROC」"
        "是两个不同的数，不能互相比较。你的项目全程用的是视频级。", "body")])

    one(P("8.4　时间维平均池化在这条谱系里的位置", "h2"))
    add([P(
        "你的模型在三段 3D 卷积之后沿时间轴取平均，这个操作在视频识别领域有专门的名字："
        "<b>Temporal Global Average Pooling（TGAP）</b>，是 3D 卷积网络在分类头之前的常规层。"
        "SlowFast（ICCV 2019）、I3D、R(2+1)D 都这么做。所以这一步<b>不是自创的</b>，有充分依据。", "body")])
    add([P(
        "时序信息也没有被平均掉：3D 卷积的时间核是 3，三段叠起来时间感受野是 7 帧，"
        "<b>帧间的变化在卷积阶段就已经被编码进通道里了</b>，平均池化只是把这些已编码的响应汇总。"
        "这和「先平均再卷积」是完全不同的两回事。", "body")])
    add([P(
        "已知的代价是<b>丢掉时间顺序</b>——正着播和倒着播，池化后的特征相同。"
        "有工作专门批评这一点并提出替代方案（比如用 BERT 替换 TGAP）。"
        "对深伪检测来说这未必是问题（闪烁、边界抖动这类痕迹不依赖顺序），但值得知道。", "body")])

    one(PageBreak())

    # -------------------------------------------------------------- chapter 9
    one(P("9　这个项目产出了什么", "h1"))
    add([P(
        "检测器没做出来，这是事实。但「实验失败」和「项目没有产出」是两件事，"
        "下面这些是可核查的清单，不是安慰。", "body")])

    one(P("9.1　方法学资产", "h2"))
    add(BUL([
        "<b>目标—供体双重隔离协议</b>，在两个数据集上实现并量化了代价"
        "（DFD 剔除 86.7%、CelebDF++ 剔除 47.0% 的 target-only 测试伪造视频）",
        "<b>划分审计</b>：77.1% 与 64.9% 这两个数字，是关于这两个基准的新观察",
        "<b>元数据基线</b>：码率单独达到 0.78，并且测了模型分数与它的相关性",
        "<b>随机初始化参照的探针协议</b>：把「特征可分性」和「训练带来的可分性」分开",
        "<b>重采样单位的敏感度分析</b>：证明区间宽度和显著性判断依赖于抽样单位",
    ]))

    one(P("9.2　被受控测量关闭的假设", "h2"))
    add([P(
        "这是「我们失败了」和「我们知道为什么失败」的区别，也是审稿人唯一会认真读的部分：", "body")])
    add(BUL([
        "学习率调度：恒定学习率在三个阶段分别劣 0.231 / 0.155 / 0.180",
        "特征塌缩：相对变异 0.970，没有塌缩",
        "特征可分但优化失败：留出探针 0.5033，t-SNE 5-NN 0.375",
        "输入模式：DFD 改用人脸裁剪后仍在随机区间内",
        "训练身份数量：CelebDF++ 削到 20 个身份只损失 0.03",
        "文件损坏：2328 条 DFD 视频逐帧解码，两类均 0.0%",
        "帧读取保真：单进程与 12 路并发下均 100% 命中",
    ]))

    one(P("9.3　还没有被检验的选择", "h2"))
    add([P(
        "这些是<b>假设不是结论</b>，不能写进论文，但对你下一步有用：", "body")])
    add(TBL(["选择", "现状", "为什么可疑"], [
        ["主干容量与预训练", "9 万参数，随机初始化", "标准主干是它的 200 倍，且都用预训练"],
        ["池化方式", "三次 4×4 平均", "低通滤波，而伪造痕迹在高频"],
        ["批大小与归一化", "batch=1 + BatchNorm", "最后一层 BN 只有 484 个元素参与统计"],
        ["最后的空间分辨率", "22×22", "融合边界在这个尺度上已经不可见"],
    ], widths=[WIDTH * 0.24, WIDTH * 0.28, WIDTH * 0.48]))
    add([P(
        "如果要继续这条路，优先级建议是：<b>先换预训练主干</b>（这一项同时解决容量、预训练、"
        "KL 主导三个问题），再重测池化，最后调批大小和归一化。"
        "贝叶斯单类头原样接上去——那才是你真正想验证的东西。", "body")])

    one(PageBreak())

    # ------------------------------------------------------------- appendix A
    one(P("附录 A　术语中英对照", "h1"))
    add(TBL(["中文", "英文", "一句话"], [
        ["张量", "tensor", "多维数组"],
        ["批 / 批大小", "batch / batch size", "一次优化步处理的样本数"],
        ["轮", "epoch", "把训练样本过一遍"],
        ["步", "step / iteration", "一次参数更新"],
        ["感受野", "receptive field", "一个输出受多大范围输入影响"],
        ["批归一化", "batch normalisation", "按通道把激活拉回均值 0 方差 1"],
        ["组归一化", "group normalisation", "同上，但不依赖批维度"],
        ["交叉熵", "cross-entropy", "分类任务的标准损失"],
        ["反向传播", "backpropagation", "高效计算梯度的方法"],
        ["学习率", "learning rate", "每步沿梯度反方向走多远"],
        ["早停", "early stopping", "验证指标不再提升就停止训练"],
        ["过拟合", "overfitting", "在训练数据上好、新数据上差"],
        ["留出集", "held-out set", "全程封存、只在最后打开一次"],
        ["选择偏差", "selection bias", "用来做选择的数据不再无偏"],
        ["线性探针", "linear probe", "冻结特征后用线性分类器测可分性"],
        ["迁移学习", "transfer learning", "把别处学到的知识用到新任务"],
        ["微调", "fine-tuning", "载入预训练权重后继续训练"],
        ["膨胀", "inflation", "把 2D 卷积核沿时间复制成 3D"],
        ["自助法", "bootstrap", "重采样估计统计量的波动"],
        ["捷径学习", "shortcut learning", "学到了有效但非本意的判别依据"],
        ["单类 / 异常检测", "one-class / anomaly detection", "只用正常样本训练"],
        ["变分推断", "variational inference", "用简单分布近似复杂后验"],
        ["均值场近似", "mean-field approximation", "假设各参数相互独立"],
    ], widths=[WIDTH * 0.2, WIDTH * 0.3, WIDTH * 0.5]))

    one(PageBreak())

    # ------------------------------------------------------------- appendix B
    one(P("附录 B　推荐阅读", "h1"))
    add([P(
        "按你目前的缺口排的优先级。每一条后面注明了它解决你哪个问题。", "body")])

    one(P("B.1　先读这两本的相关章节", "h2"))
    add(BUL([
        "<b>Goodfellow, Bengio, Courville — Deep Learning</b>（花书，有中译本）。"
        "第 6 章（前馈网络）、第 8 章（优化）、第 9 章（卷积网络）。对应本文第 2、3 章。",
        "<b>Hastie, Tibshirani, Friedman — The Elements of Statistical Learning</b>。"
        "第 7 章（模型评估与选择）。对应本文第 4 章，讲清楚为什么需要三划分。",
    ]))

    one(P("B.2　网络与训练", "h2"))
    add(BUL([
        "He et al., <b>Deep Residual Learning for Image Recognition</b>, CVPR 2016 —— ResNet，现代主干的起点",
        "Ioffe &amp; Szegedy, <b>Batch Normalization</b>, ICML 2015 —— BN 的原始论文",
        "Wu &amp; He, <b>Group Normalization</b>, ECCV 2018 —— 小批量下 BN 为什么退化，本文第 2.5 节的依据",
        "Masters &amp; Luschi, <b>Revisiting Small Batch Training</b>, arXiv 2018 —— 小批量的另一面",
        "Keskar et al., <b>On Large-Batch Training for Deep Learning</b>, ICLR 2017 —— 批大小与泛化",
    ]))

    one(P("B.3　视频", "h2"))
    add(BUL([
        "Carreira &amp; Zisserman, <b>Quo Vadis, Action Recognition?</b>, CVPR 2017 —— I3D 与膨胀技巧",
        "Tran et al., <b>A Closer Look at Spatiotemporal Convolutions</b>, CVPR 2018 —— R(2+1)D",
        "Feichtenhofer et al., <b>SlowFast Networks</b>, ICCV 2019 —— 现代视频主干",
    ]))

    one(P("B.4　表征与评估", "h2"))
    add(BUL([
        "Alain &amp; Bengio, <b>Understanding intermediate layers using linear classifier probes</b>, 2016",
        "Chen et al., <b>SimCLR</b>, ICML 2020 —— linear evaluation protocol 的标准用法",
        "van der Maaten &amp; Hinton, <b>Visualizing Data using t-SNE</b>, JMLR 2008",
        "Zhang et al., <b>Understanding deep learning requires rethinking generalization</b>, ICLR 2017",
    ]))

    one(P("B.5　深伪检测与数据问题", "h2"))
    add(BUL([
        "Rössler et al., <b>FaceForensics++</b>, ICCV 2019 —— 领域基准，DFD 通过它分发",
        "Li et al., <b>Celeb-DF</b>, CVPR 2020 —— 你用的数据集的前身",
        "Dong et al., <b>Implicit Identity Leakage</b>, CVPR 2023 —— 身份泄漏，你论文的直接相关工作",
        "Geirhos et al., <b>Shortcut Learning in Deep Neural Networks</b>, "
        "Nature Machine Intelligence 2020 —— 捷径学习的框架性论述",
        "Leyva et al., <b>Data-agnostic Face Image Synthesis Detection using Bayesian CNNs</b>, "
        "Pattern Recognition Letters 183 (2024) —— 你这个项目的原论文",
    ]))

    one(Spacer(1, 18))
    add([P(
        "<b>最后一句。</b>这个项目里检测器没有work，但你在这几个月里建立的东西——"
        "严格的划分协议、把假设一个个用受控测量关掉的习惯、"
        "对「这个数字能支持什么、不能支持什么」的敏感——"
        "这些是做研究的底层能力，比一个好看的 AUROC 更难获得，也更持久。", "body")])

    return out
